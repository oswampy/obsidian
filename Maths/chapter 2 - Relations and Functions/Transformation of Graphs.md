1) basic graphs

	![[Pasted image 20250610170607.png]]
2) graph of y = -f(x) is mirrored on x axis, (-) to the whole function completely inverts it
	![[Pasted image 20250610165831.png]]
3) graph of y = f(-x) is mirror on y axis as we invert the input (x)
	![[Pasted image 20250610165918.png]]
4) modulus function has no negative outputs so its always above x axis
	![[Pasted image 20250610165748.png]]
5) graph of mod x as input is symmetric about the y axis, so shape is mirror from the positive side(right)
	![[Pasted image 20250610165719.png]]
6) when the variable is inside mod function, we will only consider positive values from output and after opening the mod it can be both $\pm$ so its mirrored on x axis
	![[Pasted image 20250610165603.png]]
7) graph of f(x+a), when a is positive then x has to be negative to make the function zero so x has to go to the leftward side (negative) and vice versa
	![[Pasted image 20250610165538.png]]



**Q**) draw graph of $\log_{e}(x-2)$ 
since log is not defines for 0 or negative numbers, 
- x-2>0
- x>2
and we need the log function to be 0, so x-2 has to be 1
- x-2=1
- x=3
	![[Pasted image 20250610171033.png]]


	**Q**)Draw y = |2-|x-1||
- first draw |x-1| which has a v shape at x = 1
- then multiply the function with - so it will get inverted
- then add 2 so it will shift upwards twice because we are adding to the function not input otherwise it would shift right
- then mod the whole output so all negative values become positive
	![[Pasted image 20250610171511.png]]


**Q**) Draw $2-\frac{4}{|x-1|}$
- first we draw the reciprocal function
- when we subtract one from x the whole fraction is undefined at x = 1 so it doesnt touch that
- then mod the whole function to make everything positive
- then we multiply with -1 to invert
- then we multiply with 4 so the magnitude of everything becomes 4 times(ex: (2,0) becomes (8,0))
- then we add 2 to output and shift graph upwards
	![[Pasted image 20250610172050.png]]





bl
[[Functions]]