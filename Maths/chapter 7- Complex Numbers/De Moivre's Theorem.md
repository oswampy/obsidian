If  z is not in standard form, we cannot apply De Moivre's Theorem
So:
$(sinθ+icosθ)^n=cosn\left( \frac{π}{2}​−θ \right)+isin\; n\left( \frac{π}{2}​−θ \right)$
### Euler Form

$e^iθ=cos⁡θ+isin⁡θ$

$z=r(cos⁡θ+isin⁡θ) = re^{i\theta}$ 

$e^{−iθ}=cos⁡θ−isin⁡θ$ 

## Logarithm of Complex Number

Let $z = x + iy$

log of a complex no gives another complex number

Take in exponential form

$\log(re^{i\theta}) = \log r + \log e^{i\theta} = \log r + i\theta$

i) $\log(-1) = \log 1 + i\pi = i\pi$

ii) $x^i + y = z^i$, $\log y = i\log z$, $y = e^{i\ln z} = \cos(\ln z) + i\sin(\ln z)$

iii) $i^i$, $\log y = i\ln i$, $y = e^{i\ln i} = \cos(\ln i) + i\sin(\ln i)$
